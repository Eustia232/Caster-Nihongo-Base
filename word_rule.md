# Caster-Nihongo-Base 项目规划 (Word Rule)

## 1. 核心设计理念
- **目标**: 构建一个支持 SRS 复习的命令行日语学习工具。
- **扩展性**: 采用分层架构（Core, Data, Interface），为未来切换到 Vue 前端（FastAPI 后端）做准备。
- **唯一标识**: 每个单词分配唯一的 `id` (B 方案)，确保修改单词内容不影响复习进度。

## 2. 数据存储方案
- **words.yaml (核心库)**: 存储单词内容（汉字、假名、释义、词性）。
  - 格式示例：
    ```yaml
    - id: 1001
      kanji: "食べる"
      kana: "たべる2"
      meaning: "吃"
      pos: "动词"
    ```
- **progress.json (进度库)**: 存储 SRS 状态（熟练度、下一次复习时间等），由程序自动管理。
- **new.txt (录入缓冲)**: 用于批量录入新词。
  - 格式: `汉字|假名+音调|释义|词性` (A 方案)。
  - 示例: `食べる|たべる2|吃|动词`

## 3. 功能模块 (Workflow)
### 3.1 导入 (Import)
- 读取 `new.txt` 中的内容。
- 自动分配递增的唯一 ID。
- 将新词合并至 `words.yaml`。
- 在 `progress.json` 中初始化 SRS 状态。
- 清空 `new.txt`。

### 3.2 复习 (Review)
- **SRS 算法**: 类似 Anki/艾宾浩斯的间隔复习逻辑。
- **交互**: 
  - 假名包含读音数字（如 `たべる2`）。
  - 词性使用中文（如 `动词`）。
  - 终端界面遮挡答案，用户反馈“认识/模糊/不认识”后更新进度。

### 3.3 导出 (Export)
- 支持将 `words.yaml` 导出为 Markdown 表格格式。

## 4. 技术栈
- **Python 3.12+**
- **Pydantic**: 数据模型与校验。
- **Typer**: CLI 框架。
- **Rich**: 终端美化。
- **PyYAML**: YAML 解析。
